# 关于简历

## 对象程序设计(CPP)

### 面向对象与面向过程

根本的区别在于：面向过程以“动作”为中心，面向对象以“事物”为中心。

#### 面向对象的特点

- 封装：将数据封装在类中，隐藏实现细节，只提供接口。
- 继承：子类继承父类的属性和方法，并添加新的属性和方法。
- 多态：多态性允许对象以不同的方式响应相同的消息。
This part need not to be over prepared, 'cause I am familiar with it.

## 计算机图形学

### 基础

#### 变换矩阵

- 模型矩阵: 将模型从模型坐标系转到世界坐标系
- 视图矩阵: 将世界坐标系转到相机坐标系
- 投影矩阵: 将所有坐标从相机空间转到裁剪空间，去除掉屏幕外的点
- z-buffer: 用于深度排序
- z-conflict: z-buffer精度导致的深度错误。

#### 光照

- 环境光照（Ambient Lighting）：环境光永远会给物体一些颜色。
- 漫反射光照（Diffuse Lighting）：模拟光源对物体的方向性影响，是冯氏光照模型中最显著的部分。
- 镜面光照（Specular Lighting）：模拟有光泽物体上面出现的亮点。镜面光照的颜色相比于物体的颜色会更倾向于光的颜色。
- Blinn-Phong 光照模型：用半程向量计算镜面光照，而不是用法向量计算镜面光照。

#### 贴图

- 纹理映射：将纹理映射到物体上，并应用到物体的各个部分。
- 纹理坐标：纹理坐标是纹理映射的坐标，用于确定纹理中的颜色。
- 纹理贴图：描述基础纹理材质。
- 法线贴图：法线贴图用于模拟物体的曲率，并计算物体的法线向量。
- 视差贴图：视差贴图用于模拟物体的凹凸高度。

##### 光源类型

- 点光源
- 平行光

### 着色器

- 顶点着色器：顶点着色器在顶点着色阶段执行，对顶点进行变换，并计算顶点颜色。
- 片段着色器：片段着色器在片段着色阶段执行，对片段进行变换，并计算片段颜色。
- 几何着色器：几何着色器在几何着色阶段执行，对几何进行变换，并计算几何颜色。

### 八叉树场景管理

八叉树是一种空间分割算法，用于快速查找和定位对象，在大规模场景中，仅渲染视角内的对象，并且仅让最有可能可见的对象常驻内存。

### 光线追踪

- BVH树：用于快速查找和定位对象。
- 光线追踪：用于模拟光行进，并计算光行进过程中所经过的物体的颜色。
- 蒙特卡洛算法：用于估计一个函数的积分。
- 重要性采样：根据概率密度函数来选择样本点，以获得更好的估计结果。
- BRDF：双向反射分布函数，描述反射和折射的概率。
- Disney BRDF: 漫反射项 + 微表面项x菲涅尔项x几何阴影。

## 计算机视觉

为了方便你面试时应对，我把其中涉及的重要名词分成了四大板块：**图像增强、图像特征提取、模式识别基础、经典算法与特征描述子**。每个名词下面都有一句通俗的解释，方便你理解并用自己的话复述出来。

---

### 第一部分：图像增强

这部分是图像处理的“预处理”阶段，目的是让图像更适合后续的分析。

1. **空间域与频率域：**
    - **空间域**：简单理解就是图像本身。处理时直接对图像的像素点进行操作（比如改变亮度）。
    - **频率域**：把图像变换到频率成分（高频代表边缘、细节，低频代表平滑区域），在频率域处理完再变回图像。

2. **点运算与邻域运算：**
    - **点运算**：输出图像的每个像素点只由输入图像的对应点决定，和周围的点无关（比如调整亮度、对比度）。
    - **邻域运算（滤波）**：输出图像的像素点由它周围的像素点共同决定（比如模糊、锐化）。

3. **灰度变换：**
    - **图像反转**：将黑变成白，白变成黑，就像照片的底片。
    - **对数变换/幂次变换（伽马校正）**：用来拉伸图像的亮度范围。把太暗的区域调亮，或者把太亮的区域调暗，让细节显示出来。

4. **直方图及其操作：**
    - **直方图**：图像的“像素统计图”。横轴是亮度（0到255），纵轴是这个亮度有多少个像素。
    - **直方图均衡化**：一种自动调整对比度的方法。把原本集中在某个狭窄范围的直方图“拉开”，让图像看起来更清晰。
    - **直方图规定化**：比均衡化更进一步，可以人为指定输出图像的直方图形状。

5. **空间滤波：**
    - **模板/核/掩模**：一个小的矩阵（如3x3），在图像上滑动，与覆盖的图像区域进行计算。
    - **平滑滤波**：用于去噪或模糊图像。包括**均值滤波**（取周围点的平均值）和**高斯滤波**（按高斯分布取加权平均）。
    - **统计排序滤波**：一种非线性滤波。包括**中值滤波**（取周围点的中间值，对去除“椒盐噪声”很有效）、**最大值滤波**（取最亮点）、**最小值滤波**（取最暗点）。

6. **图像算术/逻辑运算：**
    - **图像加减乘除**：两幅图像对应像素点进行数学运算。
    - **图像减运算的应用**：可以**去除背景噪声**、检测**运动物体**（当前帧减背景帧）。
    - **图像加运算的应用**：多幅图像相加后取平均，可以有效地**降低随机噪声**。

---

### 第二部分：图像特征提取

这部分是从图像中提取“有用信息”的过程，将像素信息转化为数学特征。

1. **边缘提取：**
    - **梯度**：描述图像亮度变化快慢和方向的量。变化剧烈的地方（梯度大）就是边缘。
    - **梯度算子**：用于计算梯度的模板。常见的有一阶导数的**Roberts算子**、**Prewitt算子**、**Sobel算子**，以及二阶导数的**拉普拉斯算子**。
    - **Canny边缘检测**：虽然文件没提，但它是面试中必问的“最优”边缘检测算法，面试时可以主动补充，体现你的知识广度。

2. **霍夫变换：**
    - 一种“投票”算法。主要用来从边缘图像中检测出**直线**或**圆**等几何形状。原理是把图像空间中的点映射到参数空间中去“投票”。

3. **局部二值模式：**
    - 一种用来描述图像**局部纹理特征**的算子。基本思想是将中心像素与周围像素比较，大于中心点的记为1，小于的记为0，形成一个二进制数，这个数就代表了这个区域的纹理。

---

### 第三部分：模式识别基础

这部分讲的是如何用计算机对事物进行分类和识别。

1. **模式识别系统：**
    - **基本流程**：**感知（采集数据） -> 预处理（去噪、归一化） -> 特征提取（提取关键信息） -> 分类（做出决策） -> 后处理**。
    - **训练与测试**：先用已知的数据（训练集）让模型学习，再用没见过的数据（测试集）来检验模型的学习效果。

2. **贝叶斯决策理论：**
    - **先验概率**：在没有任何观察数据之前，根据以往经验判断事物属于某类的概率（比如抓到一条鱼，它是鲈鱼的概率是1/3）。
    - **类条件概率**：在已知类别的前提下，观察到某种特征的概率（比如鲈鱼的鱼身长度分布）。
    - **后验概率**：观察到某个特征后，再判断它属于某类的概率（比如抓到一条长20cm的鱼，它是鲈鱼的概率是80%）。
    - **贝叶斯公式**：连接先验、类条件、后验的公式，是很多分类器的理论基础。

3. **监督学习与无监督学习：**
    - **监督学习**：给训练数据打标签，告诉机器“这是什么”，让机器学会规律。比如人脸识别。
    - **无监督学习**：不给数据打标签，让机器自己发现数据的内在结构，把相似的聚在一起。比如**聚类**。

4. **分类器与特征：**
    - **分类器**：实现分类的算法。
    - **特征**：用于区分不同类别的属性，比如鱼的长度、颜色。
    - **特征选择**：从众多特征中挑选出最有用的那些，去掉冗余和噪声。

---

### 第四部分：经典算法与特征描述子

这部分是图像处理和计算机视觉领域里程碑式的成果，面试时提出来会很加分。

1. **SIFT：**
    - 一种非常经典的**局部特征提取算法**。它的核心优势是具有**尺度不变性**和**旋转不变性**。简单说，无论图像是放大还是缩小，是正着还是歪着，SIFT都能找到同样的关键点（如角点、边缘点），并进行匹配。

2. **LBP（之前提过）：**
    - 既可以看作是特征，也可以看作是一种算法。在面试中，可以和SIFT放在一起讲，SIFT擅长找点，LBP擅长描述纹理。

3. **Haar-like特征 + AdaBoost（人脸检测）：**
    - **Haar-like特征**：一种简单的矩形特征，可以快速计算图像的明暗差异。
    - **AdaBoost**：一种集成学习方法，把很多个弱分类器（简单的判断）组合成一个强分类器。
    - **Viola-Jones检测器**：结合了Haar特征、AdaBoost和级联结构，实现了第一代**实时人脸检测**，具有里程碑意义。

4. **Eigenface / Fisherface：**
    - 早期经典的人脸识别方法。
    - **Eigenface**：基于主成分分析，把高维的人脸图像数据降到低维，用更少的“特征脸”来代表人脸。
    - **Fisherface**：基于线性判别分析，比Eigenface多考虑了类别信息，旨在找到最能区分不同人脸的投影方向。

5. **深度学习（人脸识别）：**
    - 近十年来人脸识别的主流方法。
    - **DeepFace**、**FaceNet**、**VGGFace**：这些都是基于深度卷积神经网络的人脸识别模型。
    - **损失函数**：训练模型时用来衡量模型好坏的标准。在深度学习时代，**Softmax Loss**、**Triplet Loss**（让同一人不同照片距离更近，不同人距离更远）等概念是面试高频点。

---

### 给你的面试小建议

1. **从抽象到具体**：比如问到“什么是图像增强”，可以先说“图像增强是一类预处理技术的总称，目的是改善图像的视觉效果”，然后具体举例“比如通过灰度变换调整对比度，或者用滤波去除噪声”。
2. **横向对比**：遇到有对比的概念（如点运算 vs. 邻域运算，均值滤波 vs. 中值滤波），主动说出它们的区别，会让老师觉得你思路清晰。
3. **联系实际**：提到算法时，如果能随口说出一个应用场景会更好。比如“SIFT虽然在深度学习中用得少了，但在图像拼接、三维重建中依然很经典”。
4. **关注趋势**：在面试最后，如果被问到有什么了解，可以提一下“深度学习在图像领域的应用”，这能体现你对行业发展的关注。

## 遥感原理与图像处理

不太可能会问

## 机器学习

你提供的这些PPT是**机器学习**课程的课件，涵盖了从基础概念到经典算法的核心内容。以下是按照主题整理的重要名词，便于你在面试中快速回顾。

---

### 一、机器学习基础

1. **机器学习定义**：
    - **Arthur Samuel定义**：赋予计算机无需明确编程就能学习的能力。
    - **Tom Mitchell定义**：一个程序从经验E中学习，解决任务T，并用性能度量P来衡量，如果它在T上的性能随着E而提高，就称为学习。

2. **人工智能 vs. 机器学习 vs. 深度学习**：
    - **人工智能**：让机器模仿人类智能的广阔领域。
    - **机器学习**：AI的一个子集，让机器从数据中学习。
    - **深度学习**：机器学习的子集，基于多层神经网络。

3. **学习类型**：
    - **监督学习**：训练数据有标签（正确答案），模型学习从输入到输出的映射。如回归、分类。
    - **无监督学习**：训练数据无标签，模型发现数据内在结构。如聚类、降维。
    - **强化学习**：智能体与环境交互，通过奖惩信号学习最优策略。

4. **基本术语**：
    - **训练集/测试集/验证集**：用于训练、评估和调参的数据划分。
    - **特征/属性**：描述样本的变量。
    - **标签/目标**：要预测的值（监督学习）。
    - **过拟合/欠拟合**：模型在训练集上表现太好但泛化差（过拟合），或在训练集上表现也不好（欠拟合）。
    - **泛化能力**：模型对未见数据的预测能力。

---

### 二、回归与分类

1. **线性回归**：
    - **假设函数**：$h_\theta(x)=\theta^T x$，用特征的线性组合预测连续值。
    - **损失函数**：均方误差（MSE），衡量预测值与真实值的差距。
    - **梯度下降**：通过迭代更新参数，最小化损失函数。
    - **正规方程**：直接求解最优参数的闭式解（矩阵运算）。
    - **特征缩放**：将不同尺度的特征统一到相近范围，加速梯度下降收敛。

2. **逻辑回归（对数几率回归）**：
    - **用于二分类**，输出一个概率值（0到1之间）。
    - **Sigmoid函数**：\(g(z)=1/(1+e^{-z})\)，将线性组合映射到概率。
    - **决策边界**：线性或非线性的分界线，由参数决定。
    - **损失函数**：交叉熵损失（对数损失），鼓励正确分类的概率高。
    - **多分类**：通过一对多（One-vs-all）策略，为每个类别训练一个二分类器。

3. **正则化**：
    - **目的**：防止过拟合，通过向损失函数添加惩罚项，限制参数大小。
    - **L2正则化（岭回归）**：惩罚参数的平方和，使参数趋近于零但不等于零。
    - **L1正则化（Lasso）**：惩罚参数的绝对值，可使部分参数为零，实现特征选择。

---

### 三、神经网络

1. **神经元模型**：模拟生物神经元，输入加权求和后通过激活函数输出。
2. **激活函数**：引入非线性，如Sigmoid、Tanh、ReLU。
3. **网络结构**：
    - **输入层**：接收原始特征。
    - **隐藏层**：中间层，进行特征变换。
    - **输出层**：输出最终预测。
4. **前向传播**：从输入到输出的计算过程。
5. **反向传播**：根据损失函数计算梯度，逐层更新权重的算法。
6. **多层感知机**：包含至少一个隐藏层的全连接神经网络，可以解决非线性问题。
7. **深度神经网络**：多个隐藏层的神经网络。

---

### 四、无监督学习

1. **聚类**：将数据分成若干个组（簇），使得同一簇内样本相似度高，不同簇相似度低。
    - **K-Means**：最常用的聚类算法。随机初始化K个中心，迭代分配样本到最近中心并更新中心。
    - **K的选择**：肘部法则（看畸变程度随K变化的拐点）或根据业务需求。
    - **DBSCAN**：基于密度的聚类，可以发现任意形状的簇，并识别噪声点。

2. **降维**：将高维数据映射到低维空间，保留主要信息。
    - **主成分分析**：寻找数据方差最大的方向（主成分），将数据投影到这些方向上，实现降维。可用于数据压缩、可视化、加速后续算法。
    - **应用**：数据压缩（减少存储）、加速监督学习、可视化（降到2D或3D）。

---

### 五、异常检测

1. **概念**：识别与大多数数据显著不同的样本（异常点）。
2. **密度估计**：假设正常数据服从某种分布（如高斯分布），计算新样本出现的概率，若概率低于阈值则判为异常。
3. **高斯分布**：单变量高斯分布用于单个特征，多变量高斯分布可捕捉特征间相关性。
4. **异常检测 vs. 监督学习**：异常检测用于正样本极少、异常模式多样的情况（如欺诈检测）；监督学习用于正负样本都较多且均衡的情况（如垃圾邮件分类）。

---

### 六、贝叶斯推断与分类

1. **贝叶斯决策论**：在概率框架下做决策，最小化期望风险。
2. **先验概率**：基于历史数据或经验得到的类别概率。
3. **后验概率**：观察到样本后，该样本属于某类的概率。
4. **类条件概率（似然）**：给定类别下，样本出现的概率。
5. **极大似然估计**：假设数据服从某种分布，估计使数据出现概率最大的参数。
6. **朴素贝叶斯分类器**：
    - **核心假设**：属性条件独立（给定类别，各特征相互独立）。
    - **拉普拉斯平滑**：避免因训练样本中某属性值未出现而导致概率为零的问题。
7. **半朴素贝叶斯**：放松独立性假设，允许属性间存在一定的依赖关系（如SPODE、TAN）。
8. **贝叶斯网络**：用有向无环图表示变量间的依赖关系，结合条件概率表刻画联合分布。

---

### 七、决策树

1. **决策树模型**：树形结构，内部节点表示属性测试，分支表示测试输出，叶节点表示类别。
2. **划分选择**：选择最优属性进行分裂。
    - **信息熵**：度量样本集合纯度的指标。
    - **信息增益**：划分前后熵的减少量，ID3算法使用。
    - **增益率**：对信息增益进行归一化，C4.5算法使用。
    - **基尼指数**：衡量从数据集中随机抽取两个样本类别不一致的概率，CART算法使用。
3. **剪枝处理**：防止过拟合。
    - **预剪枝**：在树生成过程中提前停止分裂。
    - **后剪枝**：先生成完整树，再自底向上评估是否合并子树。
4. **连续值处理**：将连续属性离散化，如二分法。
5. **缺失值处理**：如何在属性缺失时选择划分属性和分配样本。
6. **多变量决策树**：内部节点不再是单个属性测试，而是属性的线性组合，可简化树结构。

---

### 八、其他重要概念

1. **模型评估指标**：
    - **回归**：均方误差、平均绝对误差等。
    - **分类**：准确率、精确率、召回率、F1分数、ROC曲线、AUC。
2. **偏差与方差**：偏差描述模型预测值与真实值的差异（欠拟合），方差描述模型对数据波动的敏感性（过拟合）。
3. **集成学习**：结合多个模型提升性能，如随机森林、梯度提升树。
4. **特征工程**：从原始数据中构造、选择、转换特征，是提升模型效果的关键步骤。

马尔科夫（Andrey Markov）是一位俄国数学家，以其在随机过程领域的研究而闻名。下面系统性地介绍与马尔科夫相关的核心概念、模型和应用。

### 九、马尔科夫

### 1. 核心思想：马尔科夫性质

一切马尔科夫相关概念的基础是**马尔科夫性质**，也被称为“无记忆性”。其核心思想是：

> **系统的未来状态，只取决于当前状态，而与过去状态无关。**

用数学语言表达为：
$$
P(X_{t+1} = x | X_1 = x_1, X_2 = x_2, ..., X_t = x_t) = P(X_{t+1} = x | X_t = x_t)
$$

通俗理解：假设你要预测明天的天气，只需知道今天的天气就足够了。至于昨天、前天是什么天气，对于预测明天天气没有任何额外的帮助。

### 2. 基础模型：马尔科夫链

马尔科夫链是最简单、最基础的马尔科夫模型。它描述了一个系统在不同状态之间随时间转移的过程。

- **组成要素**：
  - **状态空间**：系统所有可能的状态集合，例如 {晴天，阴天，雨天}。
  - **转移概率**：从一个状态转移到另一个状态的概率。这些概率通常用一个矩阵来表示，称为**转移概率矩阵**。
  - **初始分布**：系统在初始时刻所处状态的概率分布。

- **例子：一个简单的天气模型**
  - 状态：晴天，阴天，雨天
  - 转移概率矩阵：
    - 如果今天是晴天，明天是晴天的概率是 0.7，是阴天的概率是 0.2，是雨天的概率是 0.1。
    - 如果今天是阴天，明天是晴天的概率是 0.3，是阴天的概率是 0.5，是雨天的概率是 0.2。
    - 如果今天是雨天，明天是晴天的概率是 0.2，是阴天的概率是 0.3，是雨天的概率是 0.5。
  - 有了这个模型，就可以根据今天的天气，推算出未来几天天气的概率分布。

### 3. 扩展模型：隐马尔科夫模型

隐马尔科夫模型是马尔科夫链的一个重要扩展，它增加了“隐层”的概念。在这个模型中，系统的真实状态是**隐藏的、不可见的**，我们只能观察到与这些隐藏状态相关的**观测值**。

- **核心要素**：
  - **隐藏状态链**：一个符合马尔科夫性质的、不可见的状态序列。
  - **观测值**：由隐藏状态生成的可观测数据。
  - **三个关键概率矩阵**：
        1. **初始概率分布**：隐藏链的初始状态概率。
        2. **状态转移概率矩阵**：隐藏状态之间的转移概率。
        3. **发射概率矩阵**：在某个隐藏状态下，生成各个观测值的概率。

- **一个经典的例子：朋友的心情与天气**
  - **隐藏状态**：天气 {晴天，雨天} （无法直接看到）
  - **观测值**：朋友的活动 {散步，购物，打扫} （可以看到）
  - **模型描述**：
    - 天气（隐藏状态）按马尔科夫链变化（今天的天气影响明天的天气）。
    - 不同的天气下，朋友做不同活动的概率不同。例如，晴天散步的概率高，雨天打扫的概率高。
  - **应用**：通过连续几天观察朋友的活动（观测序列），可以推断出最有可能的天气变化序列（隐藏状态序列），或者预测朋友明天最可能做什么活动。

- **要解决的三大经典问题**：
    1. **评估问题**：给定模型和观测序列，计算这个观测序列出现的概率。
    2. **解码问题**：给定模型和观测序列，找出最有可能产生这个观测序列的隐藏状态序列。
    3. **学习问题**：给定观测序列，如何调整模型参数，使得这个观测序列出现的概率最大。

### 4. 控制与决策模型：马尔科夫决策过程

马尔科夫决策过程是在马尔科夫链的基础上，引入了**动作**和**奖励**的概念，用于描述智能体与环境交互并进行决策的过程。

- **核心要素**：
  - **状态**：环境的状态。
  - **动作**：智能体可以采取的行动。
  - **转移概率**：在某个状态执行某个动作后，转移到下一个状态的概率。
  - **奖励函数**：在某个状态执行某个动作后，智能体获得的即时奖励。
  - **策略**：智能体在某个状态下选择哪个动作的规则。

- **与马尔科夫链的区别**：马尔科夫链的转移是概率性的、被动的；而 MDP 的转移是智能体通过选择动作来主动影响的，目标是最大化长期累积的奖励。

- **应用**：这是**强化学习**的理论基础，广泛应用于机器人控制、游戏AI、自动驾驶、资源调度等领域。

### 总结

| 模型 | 核心特点 | 典型应用 |
| :--- | :--- | :--- |
| **马尔科夫链** | 状态可见，状态间按概率转移 | 预测天气、网页排名、基因序列分析 |
| **隐马尔科夫模型** | 状态隐藏，观测值可见 | 语音识别、自然语言处理、生物信息学 |
| **马尔科夫决策过程** | 引入动作和奖励，用于决策 | 强化学习、机器人控制、游戏AI |

马尔科夫模型提供了一套强大的数学框架，用于理解和建模随时间变化的随机系统，在现代科学和工程中发挥着重要作用。如果你对某个具体模型的应用或数学细节感兴趣，可以进一步探讨。

---

## 数据可视化

基于Vue,Three.js实现了一个点云可视化web站点

# 关于英语口语

## 自我介绍

Good afternoon, dear professors, It's an honor to attend this interview. My name is Lou Yuxuan, and I’m from Fuzhou.I graduated from Shenzhen University, where I majored in Geo-Informatics Engineering (Smart City).  

During my undergraduate studies, I strictly take every course and successfully passed the CET4 and CET6.I actively participated in academic competitions and was honored with the National Second Prize in the China Undergraduate Mathematical Contest in Modeling. Beyond coursework, I initiated several personal projects and I have read serveral brilliant opensource projects. Writing blogs is another routine when I am learning. It's good to review what I have learned via blogging, and It's better if my blogs help other newcommers like me. My blogs have received over fifteen thousand views by now. 

I’m deeply passionate about AI technology.Throughout my undergraduate years, I applied various machine learning techniques to solve real-world problems, which profoundly fascinated and influenced me, and triggered my interst in what's begind those algorithms. I want to figure out how do those model work and why do those model work.That’s why I’m eager to pursue further research in this field. Thank you.

## 关于408基础知识

1

## 关于项目

## 蔬菜自动定价与补货方案

数据：百万条成交数据

1. 分析并建模不同菜品在一天时间内的价格变化和损耗率，并建模单次购买菜品重量的高斯分布系数。
2. 分割交易行为，建模客流量在一天内，一周内，一年内的变化情况。
3. Holt-Winter三次指数平滑法时序预测未来变化
4. Apriori算法得到菜品的贝叶斯网络。
5. 基于数据反向估计各菜品的利润率。
6. 总利润可以看做每个客户提供利润的总和，用蒙特卡洛算法来估计
7. 将客户的购买行为建模为马尔科夫模型，即连续购买商品时，下一个购买商品概率分布由上一个购买的商品决定。该分布参考贝叶斯网络。购买商品数量从该商品的高斯分布中抽样。

## 大创项目

**无迹卡尔曼滤波（Unscented Kalman Filter，UKF）**是一种用于非线性系统状态估计的递归滤波器。它在经典卡尔曼滤波（KF）的框架基础上，利用**无迹变换（Unscented Transformation，UT）**来处理非线性问题，避免了扩展卡尔曼滤波（EKF）中线性化带来的误差。

### 核心思想

- **问题背景**：当系统模型或观测模型为非线性时，状态的概率分布经过非线性变换后难以精确求解。EKF通过泰勒展开进行一阶线性近似，但在强非线性下误差较大。
- **无迹变换**：UKF不直接线性化非线性函数，而是通过一组精心选取的**Sigma点**来近似状态的分布。这些Sigma点能够捕获状态的均值和协方差，经过非线性函数传播后，加权重构出新的均值和协方差，从而更准确地描述变换后的分布。

### 主要步骤

1. **初始化**：设定初始状态均值 \(\hat{x}_0\) 和协方差 \(P_0\)。
2. **生成Sigma点**：根据当前状态的均值和协方差，生成一组确定性采样点（通常为 \(2n+1\) 个，\(n\) 为状态维数），并分配相应的权重。
3. **预测阶段**：将每个Sigma点通过非线性状态方程传播，得到预测的Sigma点集；再通过加权合并计算预测状态均值和协方差。
4. **更新阶段**：将预测的Sigma点通过非线性观测方程传播，得到观测Sigma点集；计算预测观测均值、协方差以及状态与观测的互协方差；最后结合实际观测值，按照卡尔曼增益更新状态估计。

### 特点与优势

- **无需计算雅可比矩阵**：避免了EKF中复杂且易出错的导数求解。
- **精度较高**：对于非线性较强的系统，UKF的近似精度通常优于EKF（至少达到二阶泰勒精度）。
- **计算量适中**：虽然比EKF略大，但远低于粒子滤波等蒙特卡洛方法，适用于实时应用。

### 典型应用

- 目标跟踪（雷达、视觉）
- 导航与定位（GPS/INS组合导航）
- 机器人状态估计（SLAM）
- 金融时间序列分析

总之，无迹卡尔曼滤波通过无迹变换巧妙地处理非线性，在保持卡尔曼滤波框架简洁性的同时，显著提升了非线性估计的准确性和鲁棒性，成为工程实践中广泛使用的非线性滤波工具。

我们来详细解释一下**交叉注意力（Cross-Attention）**。

这是一个在深度学习，特别是自然语言处理和计算机视觉领域非常重要的概念，它是Transformer架构能够处理不同模态数据（如文本和图像）的关键。

### 一句话概括

**交叉注意力是一种机制，允许一个序列（比如要生成的翻译句子）在处理自身信息时，去“关注”或“参考”另一个完全不同的序列（比如原始的源语言句子），从而从后者中提取有用的信息。**

为了帮助你更直观地理解，可以把它想象成一个**翻译过程中的查词**：

- 你是一个译者，正在**写一个中文句子**（目标序列）。
- 你的手边有一本**英文说明书**（源序列）。
- 当你在写中文的某个词时，你需要在英文说明书里找到对应的单词来确保翻译准确。你写的每一个中文词，都会去“关注”英文说明书的不同部分。
- 这个“关注”的过程，就是交叉注意力。

---

### 核心概念：与自注意力的对比

要理解交叉注意力，最好的方法是把它和它的“兄弟”——**自注意力（Self-Attention）** 进行对比。

| 特性 | **自注意力** | **交叉注意力** |
| :--- | :--- | :--- |
| **输入来源** | 一个单一的输入序列 | **两个不同**的输入序列 |
| **关注对象** | 序列内部的元素之间互相“关注”（词与词自己比） | 一个序列（如查询）去“关注”另一个序列（如键/值） |
| **经典应用** | BERT、文本分类、文本自身的特征提取 | 机器翻译（解码器）、文生图模型（如Stable Diffusion） |
| **通俗类比** | 一个小组内部开会，成员之间互相讨论，完善想法 | 一个小组（目标组）去另一个小组（源组）的会议室“取经” |

---

### 工作原理（技术细节）

在Transformer模型中，注意力机制通常涉及三个基本元素：**查询（Query, Q）**、**键（Key, K）** 和**值（Value, V）**。

在**交叉注意力**中：

1. **查询（Q）来自哪里？**
    - **查询（Q）** 来自于 **目标序列**（Target Sequence）。例如，在机器翻译的解码器中，查询来自于你正在逐个生成的“中文句子”部分。

2. **键（K）和值（V）来自哪里？**
    - **键（K）** 和 **值（V）** 都来自于 **源序列**（Source Sequence）。例如，它们来自于编码器处理过的“英文句子”。

**计算过程：**

1. **计算注意力分数**：用目标序列的每一个位置的**查询（Q）**，去和源序列所有位置的**键（K）** 进行相似度计算（通常用点积）。这一步的目的是找出：对于当前正在生成的这个词（如“猫”），源句子中的哪些词（如 "The", "cat", "sat"）是最相关的。
2. **归一化**：通过Softmax函数将这些分数转换成概率分布，得到注意力权重。权重越高，表示应该给予源序列中对应位置更多的关注。
3. **加权求和**：用上一步计算出的注意力权重，对源序列的**值（V）** 进行加权求和。权重越大的位置，其值对最终输出的贡献就越大。

**最终输出是一个融合了源序列信息的、与当前查询（目标序列位置）紧密相关的上下文向量。**

---

### 生活中的例子：看图写作

假设任务是根据一张图片写一段文字描述。

- **源序列**：图片经过处理后的一系列图像块（Patches），它们是键（K）和值（V）。
- **目标序列**：你正在一个词一个词写出来的句子，比如“一”、“只”、“黑”、“猫”。每个词的状态就是查询（Q）。

在写“猫”这个词的时候，交叉注意力机制会：

1. 将“猫”这个查询（Q）与图片中所有图像块（K）进行比对。
2. 它可能会发现，图片中有一个图像块与“猫”的相似度非常高（注意力分数高）。
3. 于是，它会让这个图像块的信息（V）更多地融入到“猫”这个词的最终表示中，确保这个词准确地描述了图片内容。

### 总结

**交叉注意力是一种强大的信息融合机制，它通过让一个序列（查询）去“查阅”另一个序列（键/值）的信息，从而将两个不同来源的信息动态地、有选择地结合起来。** 这使得模型能够处理“一个输入，一个输出”但两者形式不同（如语音转文字、视频理解）或者“一个输入控制另一个输出的生成”这类复杂的任务，是现代多模态AI模型的基石。

## 毕设

LevelDB 最根本的特点是采用了 **LSM (Log-Structured Merge) 树** 的存储架构。这种设计将昂贵的随机写操作转换为顺序写，从而奠定了其高性能写入的基础

数据库快照是一种高效、轻量级的即时数据视图技术。它通过“写时复制”机制，在创建时几乎不占用额外空间和时间，却能提供一个数据在特定时刻的只读副本。它主要用于快速恢复数据、分担查询负载，是现代数据库系统中不可或缺的数据保护和管理工具

**Huffman编码**是一种用于无损数据压缩的贪心算法，通过为输入字符分配可变长度编码来实现高效存储。其核心思想是：**出现频率越高的字符，分配越短的编码；频率越低的字符，分配越长的编码**，并且所有编码满足**前缀码**性质（即任何一个字符的编码都不是另一个字符编码的前缀），从而保证解码时无歧义。

## QA

好的，我来为你提供这些问题的参考回答思路。请注意，这些问题很多是开放性的，我的回答只是提供一个思考方向，你可以根据自己的实际理解和项目经验进行调整和深化。

## 第一组：数据结构与算法 + 数学建模项目

**1. 哈希树在Apriori算法中的作用和优势？**

- **参考回答**：在Apriori算法中，当我们生成候选k项集后，需要快速统计每个候选项集在事务数据库中的支持度计数。如果直接遍历每个事务、每个候选项集进行匹配，时间复杂度太高。哈希树的作用是**快速定位候选项集**。
- **原理**：构建一棵哈希树，所有候选项集存储在叶子节点中。当扫描一个事务时，我们递归地使用事务中的项对哈希树进行哈希，快速找到该事务可能包含的候选项集，然后只对这些候选项集进行计数。这样就避免了遍历所有候选项集。
- **其他数据结构**：除了哈希树，还可以使用**FP-Tree**（FP-Growth算法中直接避免了候选集生成），或者使用**Trie树**（前缀树）来存储候选项集，利用公共前缀减少存储空间和比较次数。

**2. 自注意力机制处理时间序列的优势？**

- **参考回答**：相比于RNN（LSTM），自注意力机制最大的优势在于**并行计算**和**长距离依赖捕捉**。
  - **并行计算**：RNN是时序依赖的，当前时间步的计算必须等待前一步完成，难以并行。而自注意力机制一次性处理整个序列，所有时间步可以并行计算，训练速度大大提升。
  - **长距离依赖**：RNN在处理长序列时，容易发生梯度消失/爆炸，导致难以捕捉远距离位置之间的关系。自注意力机制通过计算序列中任意两个位置的注意力权重，直接建立联系，无论它们相距多远，路径长度都是1，能更好地捕捉长距离依赖。
  - 此外，Transformer中的多头注意力还能让模型从不同表示子空间关注序列的不同方面。

**3. 马尔科夫链中的"状态"如何定义？状态转移概率矩阵如何计算？**

- **参考回答**：
  - **状态定义**：在蔬菜定价与补货问题中，一个"状态"可以定义为**某个时间点（如一天）的销售情况组合**。例如，可以定义为（当日销量区间，当日价格区间，当日剩余库存区间）。将连续变量离散化，每个区间组合就是一个状态。也可以更简化，只将"销售速度"分为快、中、慢三个状态。
  - **转移概率计算**：状态转移概率是指从一个状态转移到另一个状态的概率。我们可以基于历史数据计算：统计所有历史数据中，从状态i出现后，下一个时间点转移到状态j的次数，除以状态i出现的总次数。即 \( P(j|i) = \frac{Count(i \rightarrow j)}{Count(i)} \)。

**4. 如何优化Apriori算法或改用FP-Growth？**

- **参考回答**：Apriori在大数据集上确实效率较低，因为需要多次扫描数据库和生成大量候选集。优化思路：
  - **使用FP-Growth算法**：FP-Growth（频繁模式增长）只需要扫描数据库两次，它构建一棵FP-Tree，将事务数据库压缩到一棵树上，然后递归地从树上挖掘频繁项集，完全避免了候选集的生成。
  - **FP-Growth核心思想**：第一次扫描统计所有项的频率，得到频繁1项集；第二次扫描，对每个事务，将其中的项按频率降序排序，然后插入到一棵以null为根的前缀树（FP-Tree）上，每个节点记录项及其出现次数。挖掘时，从频率低的项开始，逐步构建条件模式基和条件FP-Tree，递归挖掘所有频繁项集。

**5. 蒙特卡洛模拟在低维问题中的挑战？**

- **参考回答**：即使问题维度不高，蒙特卡洛模拟仍面临一些挑战：
  - **收敛速度慢**：蒙特卡洛方法的误差收敛速度是\( O(1/\sqrt{N}) \)，N为样本数。这意味着要提高一位精度，需要增加100倍样本量。对于需要高精度的定价方案，可能需要大量的模拟次数。
  - **随机数质量**：模拟结果依赖于随机数的质量。如果使用的伪随机数发生器周期不够长或存在相关性，可能导致模拟结果偏差。
  - **结果稳定性**：由于是随机模拟，每次运行的结果可能不同。需要进行多次独立模拟并统计结果的置信区间，确保决策的稳健性。
  - **模型风险**：蒙特卡洛模拟基于我们假设的随机分布（如价格波动的分布）。如果这个假设与实际情况不符，模拟结果再好也是徒劳。

## 第二组：计算机系统基础 + EssentialDL项目

**1. CUDA线程如何映射到硬件核心？与CPU线程调度的区别？**

- **参考回答**：
  - **CUDA映射**：CUDA的线程层次是Grid→Block→Thread。当一个kernel启动时，CUDA运行时将Block分配给有足够计算资源的流多处理器（SM）。Block内的线程进一步划分为warp（通常32个线程一组），warp是SM上真正的调度单元。SM上的warp调度器负责在每个周期选择就绪的warp执行指令。
  - **与CPU的区别**：CPU线程是**重量级**的，由操作系统调度，线程切换涉及上下文保存、内核态切换等开销，每个线程有独立的堆栈和资源。CUDA线程是**轻量级**的，成千上万个线程由硬件直接管理，切换开销极小（零开销），warp内的线程以SIMD（单指令多数据流）方式执行，但允许每个线程独立分支（会有线程束分化代价）。

**2. SRAM为什么比HBM快？对深度学习的影响？**

- **参考回答**：
  - **速度差异原因**：SRAM（静态随机存取存储器）通常集成在芯片上（如作为GPU的L1/L2缓存或共享内存），距离计算核心近，且使用触发器存储数据，速度快、延迟低。HBM（高带宽内存）是片外内存，需要通过较长的总线连接，尽管带宽很高，但访问延迟远高于片上SRAM。
  - **对深度学习的影响**：深度学习的计算核心是计算，但数据搬运往往成为瓶颈（内存墙）。FlashAttention正是利用了这一原理：它将大的注意力矩阵分块，将小块加载到高速的SRAM（共享内存）中进行计算，避免频繁读写HBM。这样可以显著减少对HBM的访问次数，提升计算吞吐量，同时节省显存（因为不需要存储整个大矩阵）。

**3. 显存不足时的内存管理策略？**

- **参考回答**：当显存不足时，可以从几个层面优化：
  - **梯度检查点**：只在前向传播时保存部分关键节点的激活值，反向传播时如果需要某个中间变量的梯度，就从一个最近的检查点开始重新计算前向传播。这是用时间换空间。
  - **内存复用/就地操作**：如果某个张量后续不再使用，可以将其内存直接用于存储其他张量。PyTorch等框架的in-place操作（如`add_`）就是这种思想，但需要谨慎使用，避免影响自动微分。
  - **混合精度训练**：使用FP16存储张量和梯度，减少显存占用。同时利用FP16的计算速度。
  - **优化器状态压缩**：像Adam优化器需要保存一阶矩和二阶矩，可以尝试使用Adafactor等优化器，它通过因式分解减少矩估计的存储。
  - **数据并行与模型并行**：如果单卡放不下，就需要考虑将模型切分到多张卡上。

**4. Adam相比于SGD的优势和可能的缺点？**

- **参考回答**：
  - **优势**：
    - **自适应学习率**：Adam为每个参数计算自适应的学习率，减少了手动调节学习率的需求。
    - **动量**：结合了动量（一阶矩），能加速收敛并越过局部极小点和平坦区域。
    - **对稀疏梯度友好**：自适应学习率机制使得Adam在处理稀疏特征时表现良好，每个参数的更新步长独立。
  - **可能的缺点（泛化能力问题）**：
    - 一些研究表明，Adam在某些任务（尤其是图像分类）上的泛化能力不如带动量的SGD。可能的原因包括：自适应学习率可能导致模型收敛到泛化能力较差的尖锐极小点；Adam的梯度更新更平滑，可能错过某些对泛化有帮助的"险峻"路径。
    - 此外，Adam的二阶矩估计在训练初期可能不稳定，导致步长过大，虽然有权重偏置校正，但仍需注意。

**5. 计算图结构和控制流的处理？**

- **参考回答**：
  - **计算图结构**：我的计算图采用**动态图**的方式，类似于PyTorch。每个张量（Tensor）是一个节点，记录它的数据值、是否需要梯度、以及产生它的操作（grad_fn）。每个操作（如加法、矩阵乘法）是一个函数，在计算过程中同时构建图。前向传播时，执行运算并记录操作；反向传播时，从损失函数节点开始，沿着grad_fn递归计算梯度。
  - **控制流处理**：动态图的优势在于，Python原生的控制流（if-else, while循环）直接生效。因为图是在运行时逐行构建的，遇到分支时只会构建实际执行的那一条路径，循环也是如此，相当于将循环展开。在反向传播时，只需要沿着记录的路径回传即可。静态图则需要将控制流作为图的节点（如tf.cond, tf.while_loop）显式加入图中。

## 第三组：机器学习/深度学习 + 科研项目

**1. 为什么选UKF而不是EKF？无迹变换如何近似分布？**

- **参考回答**：
  - **为什么选UKF**：EKF通过一阶泰勒展开线性化非线性函数，对于强非线性系统，这种近似误差较大，可能导致滤波发散。UKF采用无迹变换，它不对非线性函数做近似，而是对**概率分布**做近似。它选取一组确定性采样点（Sigma点），让这些点经过非线性变换，然后通过加权统计得到变换后的均值和协方差。对于高斯分布，UKF理论上可以捕捉到三阶矩的精度，而EKF只有一阶精度。
  - **无迹变换**：假设状态向量x的维数为n，均值为\( \bar{x} \)，协方差为P。无迹变换会选取\( 2n+1 \)个Sigma点，这些点的选取方式使得它们的样本均值和协方差恰好等于\( \bar{x} \)和P。每个点有对应的权重（计算均值的权重和计算协方差的权重）。将这些点通过非线性函数\( y=f(x) \)后，用加权方式计算y的均值和协方差。

**2. 交叉注意力的工作原理？Q/K/V如何定义？**

- **参考回答**：
  - **工作原理**：交叉注意力允许一个序列（Query）从另一个序列（Key/Value）中提取信息。它计算Query中的每个元素与Key中每个元素的相似度（注意力权重），然后用权重对Value进行加权求和，从而将另一个序列的信息融合到Query序列中。
  - **Q/K/V定义**：在我的项目中，假设我们要将WiFi的RSSI信息融合到IMU的惯导信息中。我会这样设计：
    - **Query**：来自IMU的特征序列。我们希望为每个时间步的IMU特征，从WiFi序列中找到相关的信息来辅助定位（比如在信号好的地方修正累积误差）。
    - **Key/Value**：来自WiFi的RSSI序列。Key用于和Query计算相似度（哪些时刻的WiFi信息与当前IMU状态相关），Value则是这些WiFi特征本身，用于加权融合。
  - **设计理由**：这样可以让模型动态地决定：在信号强的区域，多依赖WiFi修正；在信号弱或无WiFi区域，注意力权重会自动降低，主要依赖IMU进行航位推算。

**3. 定位模型的评价指标？**

- **参考回答**：
  - **位置精度指标**：
    - **均方根误差**：最常用，衡量整体误差水平。
    - **平均绝对误差**：对异常值不那么敏感。
    - **累积分布函数**：比如定位误差小于1米、2米、5米的比例，能直观反映精度分布。
  - **其他重要指标**：
    - **实时性/延迟**：融合算法在嵌入式设备或手机上的运行时间，是否满足实时要求。
    - **鲁棒性**：在信号中断、WiFi突变等情况下的表现。可以通过人为切断部分信号或添加噪声来测试。
    - **轨迹平滑性**：定位点是否跳变剧烈，可以用加速度或曲率来衡量。
    - **能效**：算法对手机电量的消耗。

**4. Transformer处理IMU时序信号时，位置编码还重要吗？**

- **参考回答**：**非常重要，甚至比在NLP中更关键。**
  - **失去的信息**：Transformer的自注意力机制是**置换不变**的，如果不加位置编码，模型会把一个时间序列当成一个无序的集合来处理。对于IMU数据，时序顺序至关重要：加速度和角速度的顺序直接对应着运动的先后，颠倒顺序会完全改变运动轨迹。模型会失去对运动方向、速度变化、时间间隔的感知。
  - **位置编码的作用**：通过加入位置编码，模型才能区分"早先时刻的IMU读数"和"当前时刻的IMU读数"，才能理解运动的动态过程。在NLP中，句子顺序颠倒可能还能猜出大意，但在时序信号中，顺序颠倒就是完全错误的物理过程。

**5. 对RSSI序列做了哪些预处理？**

- **参考回答**：RSSI信号确实波动剧烈，我会做以下预处理：
  - **中值滤波/滑动平均**：去除瞬时的高斯噪声和尖峰。中值滤波对去除椒盐噪声（突发干扰）特别有效。
  - **异常值剔除**：设定一个合理的RSSI范围（如-100dBm到-30dBm），超出范围的视为无效数据，可能直接丢弃或用前一时刻的有效值填充。
  - **缺失值插补**：如果某个时刻没扫描到WiFi信号，可以用线性插值、前向填充或基于路径损耗模型估算。
  - **差分/对数转换**：有时会计算RSSI的变化量（一阶差分），因为绝对RSSI受环境因素影响大，但变化量可能与人的移动相关。也可能做对数转换，因为信号强度与距离的对数成反比。
  - **指纹归一化**：在输入模型前，对每个AP的RSSI进行归一化，使其均值为0、方差为1，加速收敛。

## 第四组：数据库/存储 + 毕业设计

**1. LSM树的基本思想及与B+树的对比？**

- **参考回答**：
  - **LSM树思想**：LSM树（日志结构合并树）的核心思想是**将随机写转化为顺序写**，以大幅提升写入性能。它由内存中的MemTable（通常用跳表实现）和磁盘上的多层SSTable组成。所有写入先写到MemTable（顺序写内存）和WAL（顺序写磁盘日志），当MemTable满后，将其刷到磁盘作为不可变的SSTable。后台定期对SSTable进行合并（Compaction），清理冗余数据并保持有序。
  - **与B+树的对比**：
    - **写性能**：LSM树远优于B+树，因为B+树的随机写需要多次磁盘寻道，LSM树是顺序写。
    - **读性能**：B+树通常优于LSM树，因为B+树的数据是有序且几乎在原地更新，读路径短；LSM树需要从多层级SSTable中查找，可能读放大。
    - **空间放大**：LSM树因为有多份数据副本，空间放大较严重，但通过合并可以控制。B+树空间利用率高。

**2. 如何实现"查询路径接近Huffman编码最优"？**

- **参考回答**：这个设计思路非常巧妙。核心思想是**根据拓扑关系出现的频率，动态调整Key的编码长度**，让高频出现的查询在遍历索引树时走更短的路径。
  - **Key编码设计**：假设我们用一个变长编码来表示一个空间对象的ID及其与其他对象的关系。对于查询路径，可以理解为从根节点到目标对象的索引路径。
  - **Huffman思想**：统计历史查询中，各类拓扑关系（如包含、相邻、相交）的发生频率。频率越高的拓扑关系，我们给它在索引树中分配**更短的编码前缀**。例如，"包含"关系占50%，就用前缀"0"；"相邻"关系占30%，用前缀"10"；"相交"占20%，用前缀"11"。这样，在处理一个高频查询时，索引系统能更快地沿着短前缀定位到相关数据块，减少了IO次数和比较次数，使得平均查询路径长度最短。
  - **K-V实现**：在LevelDB中，Key可以设计为`[拓扑关系编码][对象ID1][对象ID2]`。这样，同一种拓扑关系的所有数据在物理存储上会聚簇在一起，查询某类关系时，只需扫描对应的前缀区间。

**3. 如何存储"包含"关系以高效查询"所有包含某物体的物体"？**

- **参考回答**：对于"A包含B"这种关系，我们可以设计两种查询模式：查询A包含哪些物体，以及查询哪些物体包含B。
  - **存储模式**：可以设计两种Key-Value组合：
        1. **正向索引**：Key = `"包含:" + A的ID + ":" + B的ID`，Value = 空或附加属性。这样通过`"包含:A的ID"`的前缀扫描，可以快速找到A包含的所有物体。
        2. **反向索引**：Key = `"被包含于:" + B的ID + ":" + A的ID`，Value = 空。这样通过`"被包含于:B的ID"`的前缀扫描，可以快速找到所有包含B的物体（即B的父容器）。
  - **查询优化**：如果只需要查询"所有包含B的物体"，直接使用反向索引，在LevelDB中就是一次前缀范围查询，非常高效。这种设计实际上是一种空间换时间的思路，通过冗余存储来加速双向查询。

**4. 分布式部署的挑战：分区和一致性？**

- **参考回答**：
  - **数据分区**：
    - **基于空间范围的分区**：将地理空间划分为网格或四叉树，每个节点负责一个矩形区域。挑战在于**负载均衡**：城市中心区域物体密集，可能成为热点；边界区域物体稀疏，可能闲置。需要动态调整分区策略，如一致性哈希在空间上的变体。
    - **基于物体ID的哈希分区**：简单随机，但会破坏空间邻近性，导致跨节点查询增多。对于范围查询（如查询某个矩形内的所有物体），可能需要广播到所有节点然后合并结果，效率低。
  - **跨节点查询一致性**：
    - **事务问题**：如果一个操作涉及多个分区（如移动一个物体，需要更新多个节点的索引），需要保证原子性。可以使用**两阶段提交**或**基于分布式事务的协议**，但开销大。更常见的做法是设计操作单分区，或者接受最终一致性。
    - **分布式查询**：对于需要全局排序或聚合的查询（如KNN查询），需要设计分布式查询算法，协调节点向各分区发送子查询，然后合并结果并进行二次排序，这需要处理好网络开销和容错。

**5. 三维拓扑关系查询与BVH的联系和区别？**

- **参考回答**：
  - **联系**：BVH（层次包围盒）和我的三维拓扑关系构建都是**空间数据结构**，都用于加速空间查询。BVH通过构建树状层次包围盒来快速排除不可能相交的物体对，常用于碰撞检测、光线追踪。
  - **区别**：
    - **目的不同**：我的毕设目标是**持久化存储和快速查询拓扑关系**（如包含、相邻），重点在于数据库存储和索引设计。BVH通常是一种**内存中的加速结构**，用于实时计算，不关注数据的持久化和大规模分布式存储。
    - **关系类型**：BVH主要加速"相交"或"最近邻"这类几何计算。我的系统要处理更丰富的拓扑关系（如九交模型定义的八种关系），并且要支持按关系谓词查询。
    - **动态性**：BVH通常针对静态或少量动态物体重建。我的系统需要支持城市规模的高频数据更新，对写性能有要求，因此采用了LSM树这种适合写的存储引擎。
    - **应用场景**：BVH用于图形学渲染、物理模拟。我的系统可用于城市规划、GIS分析、导航等需要持久化存储和查询的领域。

---
好的，我们继续输出后四组的参考回答。

## 第五组：深度学习基础 + EssentialDL项目

**1. Adam优化器的一阶矩和二阶矩的作用及偏差校正**

- **参考回答**：
  - **一阶矩估计（动量）**：一阶矩是对梯度的指数加权移动平均，相当于梯度的带权历史累积。它起到了**动量**的作用，能够加速收敛，尤其是在梯度方向一致时；同时能抑制震荡，使更新方向更平滑。
  - **二阶矩估计（自适应学习率）**：二阶矩是对梯度平方的指数加权移动平均，反映了梯度的方差（或历史梯度的大小）。它用于**自适应调整每个参数的学习率**：对于梯度较大的参数，二阶矩大，有效学习率变小，避免发散；对于梯度较小的参数，二阶矩小，有效学习率变大，加速收敛。
  - **偏差校正**：在训练初期，由于一阶矩和二阶矩初始化为0，导致估计值偏小，尤其是二阶矩。Adam通过除以`(1 - beta1^t)`和`(1 - beta2^t)`来校正，其中t是当前时间步，beta1、beta2是衰减率。随着t增大，校正因子趋近于1，偏差校正作用减弱。这样保证了在初期步长不会过小。

**2. 卷积输出尺寸计算及反向传播原理**

- **参考回答**：
  - **输出尺寸计算**：输出特征图的高度和宽度为：
      \[
      H_{out} = \left\lfloor \frac{H + 2P - K}{S} \right\rfloor + 1
      \]
      \[
      W_{out} = \left\lfloor \frac{W + 2P - K}{S} \right\rfloor + 1
      \]
      输出通道数为 \( C_{out} \)。
  - **反向传播**：
    - **误差传播到输入**：卷积层的反向传播本质上也是一个卷积操作，但需要对卷积核进行旋转（180度旋转）并对输入进行适当的填充，使输出的尺寸与输入一致。具体地，将损失对输出的梯度作为新的输入，与旋转后的卷积核进行卷积，得到损失对输入的梯度。
    - **误差传播到卷积核**：计算损失对卷积核的梯度时，需要将输入特征图与损失对输出的梯度进行卷积（实际上是对输入和输出梯度做相关操作）。具体实现中，通常使用im2col+矩阵乘法或直接卷积的变种来计算。

**3. Naive CUDA矩阵乘法与分块优化的区别**

- **参考回答**：
  - **Naive实现**：每个线程负责计算输出矩阵中的一个元素。每个线程需要读取A的一行和B的一列，但A的一行和B的一列都存储在全局内存中，且每个线程独立读取，导致大量的全局内存访问，并且**没有数据复用**。例如，计算C[i][j]时，线程需要读取A[i][k]和B[k][j] for all k，这样每个A元素会被多次读取（每个用到它的线程读一次）。
  - **分块优化**：将输出矩阵分块，每个线程块负责计算一个输出子块（例如16x16）。线程块将A中对应的子块和B中对应的子块加载到共享内存中，然后块内所有线程通过共享内存进行累加。这样，A和B的子块只需从全局内存加载一次到共享内存，后续计算完全基于共享内存，极大地**减少了全局内存访问次数**，提升了带宽利用率。同时，线程之间通过共享内存协作，可以更好地利用数据局部性。

**4. 静态计算图与动态计算图的区别，你的框架属于哪种？**

- **参考回答**：
  - **静态计算图**：先定义好计算图，然后编译执行。优点是性能优化好（图优化、内存预分配），便于部署；缺点是调试困难，控制流受限（需要特殊API）。TensorFlow 1.x、Theano是典型代表。
  - **动态计算图**：计算图在运行时动态构建，每执行一行代码就构建相应的图节点。优点是灵活、易调试，Python控制流直接生效；缺点是性能开销稍大，优化空间有限。PyTorch、TensorFlow 2.x（Eager模式）是典型代表。
  - **我的框架EssentialDL**：我实现的是**动态计算图**，因为我在计算过程中即时记录操作，反向传播时根据记录的grad_fn回溯。这样用户可以像写PyTorch一样自然地编写模型，不需要显式构建图。

**5. FlashAttention的分块和重计算原理**

- **参考回答**：
  - **分块**：FlashAttention将输入序列分块加载到SRAM中，在SRAM内计算注意力分数，不需要将整个N×N注意力矩阵写入HBM。例如，将Q、K、V分成多个块，对于每一块Q，依次加载所有K块和V块，在SRAM内计算注意力，然后写回HBM。这避免了O(N²)的显存占用，将显存复杂度从O(N²)降低到O(N)。
  - **重计算**：在反向传播时，FlashAttention并不保存前向计算的注意力矩阵（否则又要O(N²)显存）。它采用重计算策略：在反向传播中，重新加载前向的Q、K、V块，重新计算注意力分数，然后利用这些重新计算的分数来求梯度。这样用计算换内存，虽然增加了少量计算，但大幅节省了显存，使得长序列训练成为可能。

## 第六组：机器学习/数据挖掘 + 数学建模项目

**1. 如何判断强关联规则的有效性？除了支持度和置信度还有哪些指标？**

- **参考回答**：
  - **判断有效性**：出现"啤酒与尿布"这种强关联但看似不合理的规则时，不能直接采纳。需要结合业务理解，考虑是否有隐藏的混杂因素（比如促销活动、特定时间段）。可以通过**细分数据**来验证：按不同时间段、不同门店分别挖掘，看规则是否普遍存在。也可以进行**A/B测试**，在部分门店尝试组合促销，看是否真的提升销量。
  - **其他指标**：
    - **提升度**：\( Lift(A\rightarrow B) = \frac{Confidence(A\rightarrow B)}{Support(B)} \)。提升度>1表示正相关，=1表示独立，<1表示负相关。提升度能有效衡量规则的"趣味性"。
    - **杠杆率**：\( Leverage = Support(A\cup B) - Support(A)Support(B) \)。衡量A和B共现超出期望的程度。
    - **确信度**：\( Conviction = \frac{1 - Support(B)}{1 - Confidence(A\rightarrow B)} \)。值越大，规则越可靠。

**2. 节假日附近预测误差大，如何改进？**

- **参考回答**：
  - **引入外部特征**：将节假日、促销活动、天气等作为**外部回归变量**加入到模型中。例如，在LSTM中，可以将这些特征拼接到每个时间步的输入中；在XGBoost中，可以直接作为特征。
  - **使用节假日虚拟变量**：创建二进制特征表示是否是节假日，或者距离节假日的天数，让模型学习节假日的效应。
  - **分组建模**：将节假日单独作为一类，训练一个专门的节假日预测模型，或者对节假日进行特别的调整系数。
  - **数据增强**：对历史节假日数据进行加权或复制，让模型更关注这些罕见但重要的事件。
  - **残差建模**：先用普通模型预测，然后对节假日的残差单独建模（比如用GBDT）。

**3. 如何将ARIMA、LSTM、XGBoost融合成集成模型？**

- **参考回答**：Stacking是一种常用且有效的融合策略。
  - **第一层（基学习器）**：分别训练ARIMA、LSTM、XGBoost三个模型，在训练集上使用交叉验证（比如5折）生成每个模型的预测值。具体来说，对于每个样本，使用其他4折训练的模型预测该样本，这样可以避免过拟合。最终得到三个预测值作为新的特征。
  - **第二层（元学习器）**：将第一层生成的三列预测值作为新的特征，用另一个模型（如线性回归、简单神经网络或另一个XGBoost）在验证集上训练，学习如何最佳组合三个基模型的预测。最终使用训练好的元学习器对测试集进行预测。
  - **其他策略**：也可以采用简单的加权平均，权重通过网格搜索或贝叶斯优化确定。

**4. 如何将定价与补货建模成优化问题？**

- **参考回答**：
  - **问题建模**：这是一个典型的**带约束的随机优化问题**。我们可以将目标设定为最大化未来一段时间（如一个月）的总利润。决策变量是每天的**定价**和**补货量**（或补货品类组合）。
  - **约束条件**：总采购预算有限、仓库容量限制、供应商最小起订量、商品保质期（库存不能积压太久）等。
  - **求解方法**：
    - **数学规划**：如果能将目标函数和约束线性化（或凸化），可以用线性规划、整数规划、混合整数规划求解。但销量和价格之间的关系通常是非线性的（需求函数），需要近似或分段线性化。
    - **启发式算法**：当问题复杂、非凸时，可以使用遗传算法、粒子群优化、模拟退火等元启发式算法搜索较优解。
    - **动态规划**：如果问题可以按时间分解，可以用动态规划，但状态空间可能很大。
    - **仿真优化**：结合蒙特卡洛模拟和优化算法，在模拟环境中评估每个决策方案的期望利润，然后用优化算法搜索最优决策。

**5. 如何验证方案的鲁棒性？**

- **参考回答**：
  - **历史回测**：将方案应用于历史数据的不同时间段，计算平均利润和利润的波动性，并与基准方案对比。
  - **压力测试**：人为修改数据分布，比如添加噪声、改变趋势、引入突变（如疫情导致销量骤降），测试方案在这些极端情况下的表现。
  - **重采样技术**：使用Bootstrap方法对历史数据进行重采样，生成多组新的样本，在每个样本上评估方案，观察利润分布的稳定性和置信区间。
  - **灵敏度分析**：分析方案对关键参数（如需求弹性系数、成本）的敏感程度，如果参数微小变化导致利润大幅下降，说明方案鲁棒性差。
  - **对抗性验证**：构建一个"攻击"模型，试图找到使方案失效的场景，然后评估在这些场景下的表现。

## 第七组：计算机系统 + 科研项目/毕业设计

**1. TCP拥塞控制对实时定位延迟的影响及UDP丢包处理**

- **参考回答**：
  - **TCP拥塞控制影响**：TCP的拥塞控制算法（如Reno、CUBIC）在检测到丢包时，会大幅降低发送窗口（进入拥塞避免或慢启动），导致发送速率骤降。这会造成数据传输的**突发延迟**，数据可能延迟到达甚至超时重传，严重影响实时定位的流畅性，导致定位结果滞后或卡顿。
  - **UDP丢包处理**：如果使用UDP，我们需要在应用层实现可靠性机制：
    - **前向纠错**：发送冗余数据包，接收方即使丢失部分包也能恢复原始数据。
    - **重传请求**：接收方检测到丢包后，通过反馈信道请求发送方重传丢失的数据包，但会增加延迟。
    - **数据插值**：对于IMU这类高频数据，如果个别包丢失，可以用前一时刻的数据插值或预测模型填补，保证定位连续性。
    - **自适应策略**：根据丢包率调整发送频率或编码方式。

**2. 什么是写放大？LevelDB如何通过合并管理写放大？**

- **参考回答**：
  - **写放大**：指实际写入存储介质的物理数据量与应用程序请求写入的逻辑数据量的比值。在LSM树中，因为数据需要多次合并（Compaction），同一份数据可能被反复读写多次，导致写放大。
  - **LevelDB的合并策略**：
    - **层级合并**：LevelDB将数据分为多层（Level 0到Level N）。Level 0由内存直接刷入，文件可能有重叠；从Level 1开始，每一层的数据范围不重叠。当某一层的大小超过阈值时，触发合并：将该层的一个文件与下一层的重叠文件合并，生成新的文件放入下一层。
    - **这种合并策略**：通过限制合并的频率和粒度来控制写放大。LevelDB还采用了**提前合并**（Preemptive Compaction）和**延迟合并**（Lazy Compaction）的折衷。另外，通过调整每层的大小倍数（默认10倍）可以平衡读放大和写放大。
    - **压缩**：合并时还会进行数据压缩（如Snappy），减少物理存储量，间接降低写放大。

**3. 什么是线程束分化？ReLU是否会导致线程束分化？**

- **参考回答**：
  - **线程束分化**：在GPU中，一个warp（32个线程）执行同一条指令。如果线程遇到分支（如if-else），不同线程走不同路径，warp就会连续执行每个分支，屏蔽不活跃的线程，导致部分线程闲置，降低了计算资源利用率。这就是线程束分化。
  - **ReLU是否导致分化**：ReLU函数定义为`max(0, x)`。如果在一个warp中，有些线程的x>0，有些x<=0，它们在执行`max`操作时，如果实现为分支（如`if x>0 then x else 0`），就会产生线程束分化。但实际CUDA中，`max`通常是用**三目运算符**`(x>0)?x:0`，或者使用`__max`内置函数，这些操作会被编译为**无分支的指令**（如使用`setp`指令设置谓词，然后`sel`指令选择），不会导致线程束分化。因此，ReLU的实现通常是高效的。

**4. UKF假设高斯噪声，但实际WiFi噪声非高斯，改进思路？**

- **参考回答**：
  - **抗差UKF**：引入**M估计**的思想，在更新步骤中，根据新息的大小调整测量噪声协方差或对观测进行加权，抑制粗差的影响。例如，使用Huber函数对测量残差进行非线性变换，降低大残差的权重。
  - **粒子滤波**：粒子滤波不假设噪声分布，能处理任意非线性非高斯系统。但计算量较大，需要权衡实时性。可以结合UKF作为提议分布，提高粒子效率。
  - **高斯和滤波**：将非高斯分布表示为多个高斯分布的混合，用多个UKF分别处理，最后加权融合。
  - **自适应UKF**：在线估计噪声统计特性，如通过新息序列估计测量噪声协方差，使其自适应调整。
  - **异常检测与处理**：先检测WiFi信号的异常（如RSSI突变），如果是粗差，则直接丢弃该观测，只用IMU进行预测；如果是短暂干扰，则降低该观测的权重。

**5. 城市规模三维拓扑查询的系统架构设想**

- **参考回答**：
  - **存储层**：采用**分布式KV存储**（如TiKV、FoundationDB），将空间对象及其拓扑关系以KV形式存储。Key设计为变长编码，包含空间分区ID+对象ID+关系类型，支持前缀扫描。
  - **索引层**：构建**多层空间索引**：
    - **粗粒度分区**：将城市划分为均匀网格或四叉树，每个分区对应一个KV存储节点（或Region）。对象按空间位置归属到分区。
    - **细粒度索引**：在每个分区内，可以建立R-tree或四叉树的内存索引，加速本分区内的查询。
  - **计算层**：无状态查询服务，接收查询请求，解析查询谓词，根据分区信息将查询路由到相关节点，各节点并行处理子查询，最后合并结果。
  - **缓存层**：使用Redis等缓存热点数据（如城市地标建筑、高频查询区域），降低存储层压力。
  - **一致性**：对于跨分区查询，可以采用**最终一致性**，或者使用分布式事务协调器（如Google Spanner的TrueTime）来保证强一致性，但会增加延迟。
  - **查询优化**：利用Key的变长编码（类似Huffman）让高频查询在索引扫描时更快，同时利用LevelDB等引擎的前缀压缩特性减少IO。

## 第八组：算法综合 + 个人项目

**1. 将自动微分类比成编译技术问题**

- **参考回答**：非常有趣的类比。将自动微分看作一种编译过程：
  - **前向传播**类似于**语法分析+代码生成**：输入表达式（模型计算），构建抽象语法树（计算图），然后执行生成中间结果（前向计算）。前向传播实际上就是解释执行这个计算图。
  - **反向传播**类似于**反向数据流分析**或**逆向自动微分**：类似于编译器中的**活跃变量分析**或**到达定值分析**。我们可以把反向传播看作是在计算图上进行的一种**逆向模式**，从输出（损失）出发，沿着依赖链向后传播梯度信息，每个节点根据局部规则（链式法则）将梯度传递给它的输入。这和编译器优化中沿着控制流图逆向传播信息非常相似。
  - **更进一步**，可以将计算图优化（如常量折叠、表达式简化）类比为编译器中的**中间代码优化**。FlashAttention的分块策略可以看作是一种**循环分块优化**，以提高缓存局部性。

**2. Python调用C++扩展的性能开销在哪里？**

- **参考回答**：如果我用pybind11实现Python和C++的互操作，性能开销主要来自：
  - **类型转换和数据拷贝**：Python对象（如`list`, `numpy.ndarray`）与C++原生类型（如`std::vector`, `float*`）之间的转换。尤其是对于张量数据，如果每次调用都发生数据拷贝，会成为瓶颈。通常的优化是使用**零拷贝**机制，如通过pybind11的`buffer_protocol`直接访问NumPy数组的内存，避免拷贝。
  - **函数调用开销**：Python调用C++函数涉及跨语言调用，有固定的开销（如GIL管理、参数打包）。如果每个小操作（如逐元素加法）都调用C++，开销会很大。因此一般设计成将批量操作（如整个矩阵运算）封装成一个函数，减少调用次数。
  - **内存管理**：C++分配的内存需要手动管理，如果返回Python对象，需要确保内存生命周期正确，避免内存泄漏或重复释放。

**3. 设计深度学习框架时使用的设计模式**

- **参考回答**：为了让框架易于扩展，我主要使用了以下设计模式：
  - **工厂模式**：用于创建不同类型的算子（Conv2d, ReLU, Linear等）。用户通过字符串或类型标识，工厂类负责实例化对应的算子对象。这样添加新算子时只需在工厂注册，不修改现有代码。
  - **策略模式**：优化器（SGD, Adam）可以看作是一种策略，它们实现相同的接口（`step()`）。模型在训练时可以动态切换优化器。
  - **访问者模式**：在自动微分中，需要对计算图中的节点进行反向传播。可以定义一个`GradientVisitor`，遍历计算图节点，根据节点类型执行不同的梯度计算规则。
  - **观察者模式**：用于实现回调机制，如在每个训练epoch结束时记录日志、保存模型。
  - **组合模式**：神经网络本身是组合模式，一个`Sequential`容器可以包含多个层，它们都实现了相同的`forward`接口。

**4. CNN和ViT在处理图像时的本质区别**

- **参考回答**：
  - **局部性与全局性**：CNN的核心操作是卷积，它天然具有**局部归纳偏置**：每个卷积核只关注局部感受野，通过堆叠层数来扩大感受野。这使得CNN能高效捕捉局部纹理、边缘等特征，但对长距离依赖的建模能力较弱。ViT将图像分成patch，并通过自注意力直接建模**全局依赖**，一开始就能看到所有patch之间的关系，但对局部结构的先验较弱。
  - **归纳偏置**：CNN嵌入了很强的空间不变性（参数共享、局部连接），这使得CNN在数据量较少时也能很好泛化。ViT几乎没有图像特定的归纳偏置，更多依赖大量数据和预训练来学习空间结构。因此ViT通常需要在大规模数据集上预训练才能超越CNN。
  - **计算特性**：CNN的计算相对规律，易于优化和部署。ViT的自注意力计算量与序列长度的平方成正比，对高分辨率图像处理代价高，需要特殊优化（如窗注意力）。

**5. EssentialDL中FlashAttention的实现细节**

- **参考回答**：（这部分回答需要结合实际实现，以下是一个可能的描述）
  - **分块策略**：我在实现中，将输入的Q、K、V按序列长度分块，每块大小由共享内存容量决定。外层循环遍历Q的块，内层循环遍历K和V的块。对于每个Q块，我将其加载到共享内存，然后依次加载每个K块到共享内存，计算Q块与K块的点积得到局部注意力分数，然后对V块加权求和，累加到输出。整个过程在共享内存中完成，避免将完整的N×N注意力矩阵写入全局内存。
  - **反向传播重计算**：在反向传播时，我不保存前向的注意力矩阵。相反，我重新加载前向的Q、K、V块（这些保存在HBM中），重新计算注意力分数，然后利用这些分数和输出的梯度计算对Q、K、V的梯度。这样虽然增加了少量计算（重计算注意力分数），但节省了O(N²)的显存。
  - **进一步优化**：我还利用了Tensor Cores进行混合精度计算，进一步提升速度。对于长序列，还应用了稀疏注意力模式（如滑动窗口）来减少计算量。

---

好的，以下是几组相对基础的问题，涵盖了你的项目经历和核心课程内容，适合用来检验基础知识掌握程度。

---

## 第一组：数据结构与算法基础

1. **什么是哈希表？它通常用在哪些场景？你项目中有关联规则挖掘，哈希表在其中可能起到什么作用？**
2. **请解释一下Apriori算法中的“支持度”和“置信度”的含义。如果一条关联规则的提升度（Lift）等于1，说明什么？**
3. **你在数学建模项目中提到了“时序预测”，请问常用的时序预测方法有哪些？（至少说出两种）**
4. **什么是递归？你能用递归实现一个简单的功能吗（比如计算阶乘）？**
5. **项目中使用了蒙特卡洛模拟，请用通俗的语言解释一下蒙特卡洛方法的基本思想。**

---

## 第二组：操作系统与计算机组成基础

1. **进程和线程有什么区别？在Python中，多线程和多进程各自适合什么场景？**
2. **什么是虚拟内存？它解决了什么问题？**
3. **GPU和CPU在架构设计上的主要区别是什么？为什么GPU适合做并行计算？**
4. **你在EssentialDL中写了CUDA kernel，请简单描述一下CUDA的线程层次结构（grid, block, thread）。**
5. **什么是缓存（Cache）？为什么程序中局部性原理对性能很重要？**

---

## 第三组：计算机网络基础

1. **请简述TCP和UDP的主要区别。在传感器数据采集与传输的场景下，你会选择哪种协议？为什么？**
2. **什么是IP地址？IPv4和IPv6有什么区别？**
3. **HTTP和HTTPS有什么区别？HTTPS是如何保证安全性的？**
4. **如果你在开发一个分布式存储系统（类似你的毕设），需要考虑哪些网络通信方面的问题？**
5. **什么是Socket编程？简单描述一下客户端和服务器建立连接的过程。**

---

## 第四组：机器学习与深度学习基础

1. **什么是过拟合？你在项目中采用了哪些方法来防止过拟合？**
2. **简单解释一下反向传播算法的原理。它的核心数学基础是什么？**
3. **你熟悉的优化器有哪些？SGD和Adam在更新参数时的主要区别是什么？**
4. **在卷积神经网络中，卷积层的作用是什么？池化层的作用又是什么？**
5. **你在传感器融合项目中用到了UKF（无迹卡尔曼滤波），请问它与标准卡尔曼滤波（KF）的区别在哪里？**

---

## 第五组：项目相关简单问题

1. **在蔬菜定价与补货项目中，你们是如何处理数据中的缺失值和异常值的？**
2. **EssentialDL项目中，你提到“无依赖第三方库”，那么你是如何实现矩阵乘法等基本运算的？**
3. **在室内定位项目中，IMU数据（加速度、角速度）是如何用于推算行人位置的？简单描述一下航位推算的原理。**
4. **毕设中你使用了LevelDB作为存储引擎，请问LevelDB的读写特点是什么？为什么它适合你的场景？**
5. **你在深度学习框架中实现了FlashAttention优化，请简单解释一下FlashAttention的核心思想是什么。**

---
